# v1.3.1 补丁发布收口实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**目标：** 将侧边栏一致性修复纳入可复现的 `v1.3.1` 补丁发布，使代码版本、运行版本、验收文档、门禁结果和 Git 标签完全一致。

**架构：** 本次只调整发布元数据和当前基线文档，保留 `v1.3` 标签及历史验收报告。业务逻辑、数据库、GRI 结构、规则、AI、OCR、RAG 和导出语义保持冻结；所有门禁通过后才创建 `v1.3.1` 注释标签。

**技术栈：** Python 3.11、FastAPI、uv、pytest、Ruff、Next.js 16、TypeScript、Vitest、pnpm、Git

---

### Task 1：用契约测试驱动版本统一

**Files:**
- Modify: `backend/tests/api/test_openapi_contract.py`
- Modify: `backend/src/main.py`
- Modify: `backend/pyproject.toml`
- Modify: `backend/uv.lock`
- Modify: `frontend/package.json`

- [ ] **Step 1：先将版本契约测试改为 v1.3.1**

```python
async def test_openapi_exposes_v1_3_1_release_version(api_client):
    response = await api_client.get("/openapi.json")
    # 读取前后端包版本的现有代码保持不变。
    assert {
        response.json()["info"]["version"],
        backend_version,
        frontend_version,
    } == {"1.3.1"}
```

- [ ] **Step 2：运行测试并确认因现有版本为 1.3.0 而失败**

```powershell
cd backend
uv run --no-sync pytest tests/api/test_openapi_contract.py::test_openapi_exposes_v1_3_1_release_version -q
```

预期：集合差异明确显示实际值仍为 `1.3.0`。

- [ ] **Step 3：统一版本元数据**

将以下值更新为 `1.3.1`：

```text
backend/src/main.py                  APP_VERSION
backend/pyproject.toml              project.version
frontend/package.json               version
backend/uv.lock                     esg-agent-backend package version
```

`backend/uv.lock` 通过以下命令机械更新：

```powershell
cd backend
uv lock
```

- [ ] **Step 4：运行版本契约测试并确认通过**

```powershell
uv run --no-sync pytest tests/api/test_openapi_contract.py::test_openapi_exposes_v1_3_1_release_version -q
```

预期：`1 passed`。

- [ ] **Step 5：提交版本契约**

```powershell
git add -- backend/tests/api/test_openapi_contract.py backend/src/main.py backend/pyproject.toml backend/uv.lock frontend/package.json
git diff --cached --check
git commit -m "chore: bump release version to 1.3.1"
```

### Task 2：执行完整发布门禁

**Files:**
- Verify: `backend/`
- Verify: `frontend/`
- Generate ignored artifacts: `backend/data/runtime/evaluations/envision_2024/`

- [ ] **Step 1：执行后端全量测试和 Ruff**

```powershell
cd backend
uv run --no-sync pytest -q --basetemp=../tmp/pytest-v131-release
uv run --no-sync ruff check .
```

预期：全部 pytest 通过，Ruff 退出码为 0。

- [ ] **Step 2：顺序执行前端完整门禁**

```powershell
cd frontend
pnpm lint
pnpm test
pnpm typecheck
pnpm build
```

预期：lint 为 0 error；39 个测试文件、150 项测试通过；typecheck 和 production build 通过。

- [ ] **Step 3：执行 Envision v3 regeneration gate**

```powershell
cd backend
uv run --no-sync python -m src.tools.regenerate_review_csv `
  --report-id envision_2024_v3 `
  --pdf "data/reports/Envision Energy 2024-zh.pdf" `
  --profile data/reports/profiles/envision_2024.json `
  --requirements data/manifests/gri_requirement_checklist_v3.json `
  --manual-review-workbook data/review_inputs/envision_2024/manual/envision_2024_577_manual_review_second_review_Pro_20260719.xlsx `
  --final-adjudications data/review_inputs/envision_2024/adjudication/envision_2024_result_adjudication_v1.csv `
  --output data/runtime/evaluations/envision_2024/current_499_review_regenerated.csv `
  --baseline data/review_inputs/envision_2024/baselines/current_577_review_regenerated.csv `
  --audit-output data/runtime/evaluations/envision_2024/current_499_review_regenerated_audit.json `
  --diff-summary-output data/runtime/evaluations/envision_2024/current_499_review_regeneration_diff_summary.json `
  --scope-summary-output data/runtime/evaluations/envision_2024/current_499_review_scope_summary.json `
  --report-total-pages 78
```

预期：`577/499/78/0`；0 global fallback、0 新增 false disclosed、0 新增 wrong source page；audit 为 0 error、0 warning，16 条最终裁决为 0 pending。

### Task 3：写入 v1.3.1 当前基线与验收证据

**Files:**
- Modify: `README.md`
- Modify: `docs/DESIGN.md`
- Modify: `docs/DEVELOPMENT.md`
- Create: `docs/product/v1.3.1-final-release-acceptance.md`
- Preserve: `docs/product/v1.3-final-release-acceptance.md`

- [ ] **Step 1：更新三个当前基线入口**

记录以下事实：

```text
正式基线：v1.3.1
前端版本、后端版本、OpenAPI：1.3.1
前端门禁：39 个测试文件、150 项测试
补丁范围：侧边栏图片裁切与加载跳变修复
业务语义变化：0
```

- [ ] **Step 2：新建 v1.3.1 验收报告**

报告包含发布结论、与 v1.3 的差异、实际门禁结果、版本一致性、视觉验收、已知限制、冻结范围和标签信息；明确 `v1.3` 历史报告保持不变。

- [ ] **Step 3：检查文档一致性并提交**

```powershell
rg -n "当前正式发布基线|当前记录门禁|后端包、前端包|完整发布范围" README.md docs/DESIGN.md docs/DEVELOPMENT.md
git diff --check
git add -- README.md docs/DESIGN.md docs/DEVELOPMENT.md docs/product/v1.3.1-final-release-acceptance.md docs/plan/v1.3.1-release-closeout-plan.md
git commit -m "docs: close v1.3.1 release acceptance"
```

### Task 4：封版并创建不可变标签

**Files:**
- Verify: Git committed state

- [ ] **Step 1：最终核验版本、测试和工作区**

```powershell
cd backend
uv run --no-sync pytest tests/api/test_openapi_contract.py -q
cd ..
git status --short
git log -4 --oneline
```

预期：版本契约文件全部通过，工作区为空。

- [ ] **Step 2：创建并核验注释标签**

```powershell
git tag -a v1.3.1 -m "v1.3.1 product closure patch release"
git tag --points-at HEAD
git describe --tags --always --dirty
```

预期：HEAD 仅显示 `v1.3.1`，`git describe` 为 `v1.3.1`，没有 `dirty`。

- [ ] **Step 3：保留远端发布动作待用户确认**

本计划不执行 `git push` 或 `git push origin v1.3.1`。最终汇报本地提交、标签和门禁证据，由用户决定远端发布时点。
